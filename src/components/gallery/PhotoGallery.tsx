"use client";

// Compatibility re-export for older import path used in some pages:
// import PhotoGallery from "@/components/gallery/PhotoGallery";
// or { PhotoGallery } ...

export { PhotoGallery as default, PhotoGallery } from "../PhotoGallery";
