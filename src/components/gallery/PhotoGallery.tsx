// Compatibility re-export for older import paths used in some pages.
// Vercel/Next build fails if this path doesn't exist.
export { default } from "../PhotoGallery";
export * from "../PhotoGallery";
